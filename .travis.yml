language: haskell
before_install:
  - cabal install hpc-coveralls
script:
  - cabal configure --enable-tests --enable-library-coverage && cabal build
  - run-cabal-test
  - cd example
  - cabal install
after_script:
  - hpc-coveralls test
